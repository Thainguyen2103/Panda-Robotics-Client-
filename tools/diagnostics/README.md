# Local diagnostics

Developer-only microphone diagnostics live here. Run them from the repository
root so their project imports resolve correctly. The `diag_*.py` files remain
ignored because they are local experiments rather than supported test tools.
