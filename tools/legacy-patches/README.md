# Legacy one-off patches

These scripts are preserved only as implementation history. They target files
and code fragments from an older project layout and must not be run against the
current source tree. Current changes should be made directly in the relevant
module and covered by tests.
